#!/usr/bin/env python                                                           

import rospy
from sensor_msgs.msg import Imu

def detection_leg_cb(msg):
    pub = rospy.Publisher('motor_command', , queue_size=1)